services:
  healyoga:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9711:9711"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:9711/"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3
